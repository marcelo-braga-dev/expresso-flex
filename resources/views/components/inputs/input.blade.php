<div class="form-group">
    <label class="form-control-label" for="{{ $id }}">{{ $label }}</label>
    <input type="{{ $type }}" name="{{ $name }}" id="{{ $id }}"
           class="form-control form-control-alternative {{ $class }}" {{ $attributes }}>
</div>
